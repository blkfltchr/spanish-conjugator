version: '3'
services:
  prisma:
    image: prismagraphql/prisma:1.12
    restart: always
    ports:
      - '4466:4466'
    environment:
      PRISMA_CONFIG: |
        port: 4466
        # uncomment the next line and provide the env var PRISMA_MANAGEMENT_API_SECRET=my-secret to activate cluster security
        # managementApiSecret: my-secret
        databases:
          default:
            connector: postgres
            host: ec2-54-197-234-117.compute-1.amazonaws.com
            port: '5432'
            database: dcm34du8s4i8nc
            ssl: true
            user: tipmobaennapyw
            password: fa2ef7563e49a6f16378b99cf9492d16e981f6ff0a951dc5c15b524568c176ec
            migrations: true
